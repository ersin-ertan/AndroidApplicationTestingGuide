package com.nullcognition.androidapplicationtestingguide;

import android.test.AndroidTestCase;

/**
 * Created by ersin on 02/03/15 at 12:30 AM
 */
public class AndroidTestingFundementals extends AndroidTestCase {
   // which extends both Assert - all assertEquals(), fail()...
// and TestCase coutTestCases, getName, run, runBare, setName, toString,
   // reateResult, runTest, setUp, tearDown

   //  it provides methods for testing permissions, and a method that
   // guards against memory leaks by clearing out certain class references.

   // other classes are: ApplicationTestCase, InstrumentationTestCase
   // MoreAsserts - regex, ViewAssert - HasScreenCords...


   // the Mock Classes: MockApplication, MockContext, MockContentProvider,
   // MockCursor, MockDialogInterface, MockPackageManager, and MockResource
	// as stubedout versions of which methods throw UnsupportedOperationException

   // MockContentResolver, IsolatedContext - file db, RenamingDelegatingContext - provides a Context, and IsolatedContext mix
}

